#Placeholder, script to read a known md5 (or sha) hash from the user, store as a variable, then run the same hash against a filename specified by the user. Compare the two hashes and ouput the result.
##Option flags for different hash mechanisms? 
##Regex matching of known hash patterns? Each algorithm should produce a unique pattern.
